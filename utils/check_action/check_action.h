#ifndef _CHECK_ACTION
#define _CHECK_ACTION
int check_action(char * action, char * cmp_action, size_t array_length);
int check_extension(char * filename, char * cmp_action, size_t array_length);
#endif
